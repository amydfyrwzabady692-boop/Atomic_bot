# پرامپت کامل پیاده‌سازی «جم با اطلاعات»

روی پروژه فعلی بات تلگرام Atomic Shop کار کن و قابلیت زیر را بدون تغییر رفتار
موجود «جم با آیدی» پیاده‌سازی و تست کن:

1. با انتخاب «محصولات فری‌فایر»، دو مسیر «جم با آیدی» و «جم با اطلاعات» نشان
   داده شود. مسیر با آیدی باید دقیقاً محصولات، اعتبارسنجی UID، G2Bulk و پرداخت
   فعلی را حفظ کند.
2. مسیر «جم با اطلاعات» شامل عضویت هفتگی و ماهانه فری‌فایر باشد. مبنای هزینه
   هفتگی ۶۰ TRY و ماهانه ۳۰۰ TRY از دسته Apple iTunes Turkey در endpoint عمومی
   محصولات G2Bulk خوانده شود. اگر ۶۰ TRY دقیق موجود نبود، هزینه آن از قیمت
   دلاری زنده ۳۰۰ TRY به‌صورت تناسبی محاسبه شود. سپس نرخ دلار به تومان و همان
   درصد سود قابل‌تنظیم بخش جم اعمال و قیمت به هزار تومان رو به بالا گرد شود.
   در خطای API، آخرین قیمت سالم حفظ شود و قیمت حدسی ثبت نشود.
3. کاربر روش اتصال Google/Gmail، Facebook یا VK را انتخاب کند و شناسه ورود،
   رمز موقت و وضعیت 2FA را وارد کند. پیام حاوی رمز تا حد امکان از چت حذف شود.
   OTP و Recovery Code هرگز دریافت یا ذخیره نشود؛ OTP فقط هنگام ورود با کاربر
   هماهنگ شود. App Password گوگل به‌عنوان راه عبور از OAuth معرفی نشود.
4. هیچ رمز یا اطلاعات ورود حساسی به‌صورت plaintext در دیتابیس، لاگ یا اعلان
   ذخیره نشود. از Fernet و متغیر محیطی `ACCOUNT_CREDENTIALS_KEY` استفاده کن.
   رمز پس از تکمیل، لغو، انقضا یا اعلام نقص اطلاعات پاک شود. شناسه در لیست‌ها
   ماسک شود و مشاهده رمز توسط ادمین audit شود.
5. در پنل ادمین بخشی برای سفارش‌های اطلاعاتی اضافه کن: فهرست وضعیت‌ها، جزئیات
   کاربر و شماره سفارش، نمایش موقت ۶۰ ثانیه‌ای اطلاعات فقط در چت خصوصی محافظت‌شده،
   دکمه «انجام شد» و «اطلاعات ناقص». با انجام‌شدن، کاربر مطلع و رمز پاک شود.
   با ناقص‌بودن، سفارش در وضعیت نیازمند اصلاح بماند، اطلاعات قبلی پاک و پیام شامل
   شماره سفارش و آیدی پشتیبان اختصاصی برای کاربر ارسال شود.
6. آیدی پشتیبان این بخش از پنل ادمین قابل تنظیم باشد. برای کاربران بدون 2FA،
   راهنمای کوتاه و درست فعال‌سازی Google/Facebook/VK نمایش داده شود، اما ارسال
   Recovery Code ممنوع باشد.
7. migration/idempotent schema، seed محصولات، تست واحد رمزنگاری، قیمت G2Bulk،
   منوها و گردش وضعیت را اضافه کن. کل تست‌ها، `docker compose config` و build
   Docker را اجرا کن. در پایان فقط در صورت سبزبودن بررسی‌ها commit و push کن.
