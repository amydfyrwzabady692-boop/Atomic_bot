# پرامپت کامل پیاده‌سازی «جم با اطلاعات» — Atomic Bot

روی پروژه فعلی بات تلگرام Atomic Shop کار کن. رفتار «جم با آیدی» را تغییر نده.

## محصول و منو
1. با انتخاب «محصولات فری‌فایر» دو مسیر نشان بده:
   - 🆔 جم با آیدی — همان بسته‌ها، UID، G2Bulk و پرداخت فعلی
   - 🔐 جم با اطلاعات — عضویت هفتگی و ماهانه با انجام دستی امن

## قیمت‌گذاری
2. مبنای هزینه: گیفت‌کارت iTunes Turkey از G2Bulk
   - هفتگی = معادل ۶۰ TRY
   - ماهانه = معادل ۳۰۰ TRY
   قیمت فروش: `هزینه دلاری × نرخ دلار×تومان × (۱ + درصد سود)` و گرد به هزار تومان رو به بالا.
   سود این بخش مستقل از جم با آیدی باشد (پیش‌فرض ۴۰٪، قابل تنظیم در پنل).
   سود جم با آیدی مستقل بماند (پیش‌فرض ۱۰٪).

## دریافت اطلاعات از کاربر
3. روش اتصال: Google/Gmail یا Facebook یا VK.
   فیلدها: شناسه ورود + رمز موقت + وضعیت فعال بودن 2FA.
   پیام حاوی رمز را تا حد امکان از چت حذف کن.
   OTP و Recovery Code را هرگز ذخیره نکن؛ هنگام ورود با کاربر هماهنگ شود.
   برای کاربران بدون 2FA راهنمای کوتاه فعال‌سازی Google/Facebook/VK نشان بده.

## امنیت
4. رمز را با Fernet و `ACCOUNT_CREDENTIALS_KEY` رمزنگاری کن.
   بعد از تکمیل / لغو / انقضا / اعلام نقص، ciphertext را پاک کن.
   شناسه در لیست‌ها ماسک شود؛ مشاهده رمز توسط ادمین audit و موقت (≈۶۰ث) باشد.

## پنل ادمین
5. بخش «سفارش‌های جم با اطلاعات»:
   - فهرست با وضعیت آماده / ناقص / در انتظار پرداخت / تکمیل
   - جزئیات سفارش + کاربر + شماره سفارش
   - نمایش امن اطلاعات
   - ✅ انجام شد → اطلاع به کاربر + پاک کردن رمز
   - ⚠️ اطلاعات ناقص → پاک کردن رمز + پیام به کاربر با شماره سفارش و آیدی پشتیبان اختصاصی
6. آیدی پشتیبان این بخش (`credential_support_id`) از پنل قابل تنظیم باشد.
7. در مرکز عملیات، شمارنده سفارش‌های آماده‌ی بررسی نمایش داده شود.

## تست و استقرار
8. schema/seed idempotent، تست واحد vault/قیمت/منو، سپس commit/push.
9. روی VPS:
   ```bash
   cd /root/atomic_bot && git pull origin main && docker compose up -d --build
   ```
   قبل از بالا آمدن، `ACCOUNT_CREDENTIALS_KEY` را در `.env` ست کن
   (در غیر این صورت مسیر جم با اطلاعات غیرفعال می‌ماند).
