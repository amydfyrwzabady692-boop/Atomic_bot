# گزارش ممیزی ربات تلگرام Atomic Shop

تاریخ ممیزی: 2026-08-01

## محدوده و معماری

این مخزن فقط نسخه تلگرام است. نقطه ورود `bot.py`، وب‌سرور callback در
`webapp.py`، دیتابیس مستقل PostgreSQL در سرویس `atomic_bot_db` و پورت محلی
`127.0.0.1:8080` است. هیچ دیتابیس، توکن، session، درگاه یا وضعیت اجرایی با
نسخه روبیکا مشترک نیست.

جریان خرید: دریافت update تلگرام → اعتبارسنجی کاربر/محصول → ایجاد اتمیک سفارش
و snapshot قیمت → انتخاب یک روش پرداخت → تأیید اتمیک پرداخت → claim تحویل →
ارسال یک‌باره به G2Bulk → reconciliation فقط‌خواندنی → outbox اعلان کاربر و مدیر.

## باگ‌های پیدا و رفع‌شده

| شدت | علت اصلی | اصلاح |
|---|---|---|
| بحرانی | ایجاد سفارش، آیتم و اطلاعات جم در چند عملیات مستقل انجام می‌شد | `create_gem_order_atomic` و `create_sense_order_atomic` با lock محصول و rollback کامل اضافه شد |
| بحرانی | پاسخ مبهم G2Bulk می‌توانست شکست تلقی و سفارش دوباره ارسال شود | claim قبل از درخواست، وضعیت `SUBMIT_UNKNOWN`، بازیابی با remark یکتا و منع resubmit اضافه شد |
| شدید | تحویل انجام‌شده ممکن بود به‌علت شکست اعلان، ناموفق دیده شود | outbox پایدار اعلان و worker تطبیق وضعیت اضافه/تقویت شد |
| شدید | موجودی دستی هنگام ساخت سفارش رزرو نمی‌شد | رزرو اتمیک، آزادسازی در لغو/انقضا و مصرف دقیقاً یک‌باره اضافه شد |
| شدید | خطای migration در startup قابل چشم‌پوشی بود | شکست migration اکنون startup را متوقف و خطای کامل ثبت می‌کند |
| شدید | خطای خواندن وضعیت block به کاربر اجازه عبور می‌داد | کنترل دسترسی fail-closed شد |
| متوسط | محاسبه هزینه تأمین‌کننده با float انجام می‌شد | مسیر G2Bulk و سود به `Decimal` منتقل شد |
| متوسط | ترتیب محصولات ضمنی و ناپایدار بود | `SortOrder`، index و عملیات جابه‌جایی مدیر اضافه شد |
| متوسط | تاریخچه تغییر وضعیت سفارش کامل نبود | جدول/trigger تاریخچه وضعیت اضافه شد |
| متوسط | اختلاف کیف پول و ledger در پنل مشخص نبود | شمارنده reconciliation مالی به health snapshot اضافه شد |
| کم | بعضی خطاهای ثبت سود و audit خاموش می‌ماند | logging ساختاریافته با شناسه سفارش/رویداد اضافه شد |

## تغییرات دیتابیس

- `SortOrder` و index پایدار برای بسته‌های جم و سنس.
- `OrderStatusHistory` و trigger ثبت تغییر وضعیت.
- constraint/indexهای یکتای authority و reference پرداخت حفظ و بررسی شدند.
- snapshot سود با مبلغ فروش، هزینه دلاری، نرخ همان لحظه و منبع نرخ ذخیره می‌شود.
- migrationها idempotent هستند؛ شکست هر migration دیگر نادیده گرفته نمی‌شود.

## امنیت و عملکرد

- owner از `ADMIN_CHAT_ID` و مدیران تفویض‌شده از جدول مستقل تلگرام خوانده می‌شوند؛
  مدیریت مدیران فقط owner است.
- `.env` خارج از Git است و اسکن HEAD مقدار واقعی token/API key پیدا نکرد.
- callback G2Bulk دارای token امضاشده و callback زرین‌پال مبلغ، authority، مالکیت و
  reference را کنترل می‌کند.
- پردازش updateها برای هر کاربر سری و بین کاربران concurrent است؛ pool دیتابیس
  محدود و تماس تأمین‌کننده از event loop خارج شده است.
- Docker فقط پورت callback را روی loopback منتشر می‌کند، DB منتشر نیست، bot
  read-only و دارای healthcheck، log rotation و `no-new-privileges` است.

## نتیجه تست‌ها

| بخش | وضعیت | مدرک |
|---|---|---|
| Unit/Regression تلگرام | Passed | 86 تست محلی پس از این ممیزی |
| Syntax همه فایل‌های Python | Passed | compile روی 44 فایل |
| وابستگی‌های نصب‌شده | Passed | `pip check` بدون dependency شکسته |
| پرداخت و callback با mock | Passed | amount/authority/ref/تکرار/timeout و ownership |
| کیف پول | Passed | برداشت اتمیک، عدم موجودی منفی، ledger و callback تکراری |
| G2Bulk با mock | Passed | claim، پاسخ مبهم، recovery، status و outbox |
| ترتیب محصولات | Passed | صفحه اول جم/عضویت و صفحه دوم Level Up |
| concurrency درون‌پردازشی | Passed | سری‌سازی هر کاربر و هم‌زمانی کاربران متفاوت |
| تراکنش واقعی زرین‌پال/G2Bulk | Blocked | عمداً برای جلوگیری از هزینه/واریز واقعی اجرا نشد |
| migration و restore روی PostgreSQL واقعی | Blocked محلی | Docker Desktop خاموش؛ باید با چک‌لیست VPS اجرا شود |
| load test تولیدی | Blocked | بدون محیط staging و مجوز تولید اجرا نشد |

## بکاپ و بازیابی

`scripts/backup.sh` بکاپ custom-format با دسترسی محدود می‌سازد.
`scripts/restore-drill.sh` آن را در یک دیتابیس موقت restore، جدول‌های اصلی را
query و دیتابیس موقت را با trap حذف می‌کند؛ دیتابیس اصلی تغییر نمی‌کند.

## ریسک‌های باقی‌مانده

- سلامت واقعی DNS/TLS، دسترسی Telegram API، زرین‌پال و G2Bulk فقط روی VPS قابل
  تأیید است.
- هیچ نرم‌افزاری را نمی‌توان مطلقاً بدون باگ تضمین کرد؛ انتشار باید بعد از backup،
  restore drill، تست sandbox و پایش log انجام شود.
- داده شخصی/کلیدهایی که قبلاً در گفتگو یا جای دیگری منتشر شده‌اند باید طبق سیاست
  عملیاتی جداگانه rotate شوند؛ هیچ کلید واقعی در این commit قرار نگرفته است.
