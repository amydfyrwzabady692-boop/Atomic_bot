# راهنمای پنل مدیریت Atomic Bot

پس از استقرار، مدیر اصلی در تلگرام دستور `/admin` را می‌زند. امکانات پنل:

1. مدیریت بسته‌های جم: افزودن، تغییر عنوان/قیمت/موجودی، ترتیب و فعال/غیرفعال
2. مدیریت پک سنس PC و Mobile: افزودن، تغییر عنوان/قیمت/توضیح، ترتیب و وضعیت
3. مدیریت محصولات فروشگاه: افزودن، ویرایش عنوان/قیمت/موجودی، فعال/غیرفعال و حذف
4. مدیریت دسته‌بندی‌های فروشگاه
5. ساخت، فعال/غیرفعال و حذف کد هدیه و کد تخفیف
6. چت و پاسخ به تیکت‌های پشتیبانی
7. افزودن و حذف دپارتمان و تنظیم آیدی پشتیبانی
8. مشاهده و تایید/رد رسیدهای کارت‌به‌کارت سفارش و کیف پول
9. جستجوی کاربر و سفارش
10. فهرست کاربران دارای موجودی، زیرمجموعه و شماره کارت تاییدشده — با دکمه مستقیم شارژ/کسر کیف پول
11. شارژ همگانی کیف پول کاربران
12. ارسال پیام همگانی
13. تنظیم زرین‌پال، callback و فعال/غیرفعال‌کردن درگاه
14. تنظیم کارت‌به‌کارت، شماره کارت، صاحب کارت و بانک
15. ویرایش نام فروشگاه، متن خوش‌آمد و متن پشتیبانی
16. افزودن و حذف مدیران تلگرامی ربات
17. آمار کل کاربران، خریداران، موجودی، تعداد فروش و جمع فروش
18. افزودن و حذف کانال‌های جوین اجباری و لینک ورود آن‌ها
19. مرکز عملیات: رسیدهای باز، سفارش‌های گیرکرده، موجودی کم، گزارش روزانه و سلامت مالی
20. بروزرسانی دستی قیمت جم از نرخ دلار + کاتالوگ G2Bulk (علاوه بر سینک خودکار ۲۴ ساعته)
21. مدیریت سفارش‌های «جم با اطلاعات»: نمایش موقت اطلاعات رمزگذاری‌شده، ثبت
    انجام‌شدن یا نقص اطلاعات و تنظیم پشتیبان اختصاصی این سفارش‌ها

## جم با اطلاعات

- مسیر: `/admin` ← «سفارش‌ها» ← «سفارش‌های جم با اطلاعات»
- اطلاعات ورود فقط برای سفارش پرداخت‌شده و حداکثر ۶۰ ثانیه در پیام محافظت‌شده
  نمایش داده می‌شود؛ هر مشاهده در لاگ عملیات مدیر ثبت می‌شود.
- «انجام شد» سفارش را تحویل‌شده می‌کند، به کاربر پیام می‌دهد و رمز را حذف می‌کند.
- «اطلاعات ناقص» رمز قبلی را حذف و شماره سفارش و آیدی پشتیبان را برای کاربر
  ارسال می‌کند.
- آیدی مسئول این بخش از «پشتیبانی ← تنظیمات پشتیبانی ← پشتیبان جم با اطلاعات»
  قابل تغییر است.
- سود این محصولات مستقل از جم با آیدی و پیش‌فرض ۴۰٪ است. مسیر تغییر آن:
  «مالی و درگاه ← درصد سود جم با اطلاعات». هزینه پایه هفتگی ۱٫۳۲۸ دلار و
  ماهانه ۶٫۶۴ دلار است.
- سود پیش‌فرض جم با آیدی ۱۰٪ است و تغییر آن روی سود ۴۰٪ جم با اطلاعات اثری ندارد.
- OTP و Recovery Code را ذخیره نکنید؛ کد لحظه‌ای را فقط هنگام ورود با کاربر
  هماهنگ کنید.

مدیر اصلی از `ADMIN_CHAT_ID` در فایل `.env` خوانده می‌شود و قابل حذف از پنل
نیست. مدیران بعدی از بخش «تنظیمات ← مدیران ربات» با شناسه عددی تلگرام
افزوده می‌شوند. داشتن Telegram Premium شرط فنی تلگرام برای مدیرشدن نیست؛
هر حسابی که شناسه عددی آن ثبت شود دسترسی مدیر می‌گیرد.

## جوین اجباری

- مسیر: `/admin` ← «مدیریت جوین اجباری»
- کانال پیش‌فرض: `@Omid_AtomicFF`
- برای کانال عمومی، شناسه‌ای مثل `@Omid_AtomicFF` و لینک HTTPS بده.
- برای کانال خصوصی، شناسه عددی `-100...` و لینک دعوت خصوصی بده.
- ربات باید داخل هر کانال ادمین باشد؛ در غیر این صورت بررسی عضویت کاربران
  قابل اتکا نیست و پنل اجازه ثبت کانال جدید را نمی‌دهد.

## استقرار روی VPS

در پوشه پروژه روی سرور:

```bash
git pull origin main
docker compose up -d --build
docker compose logs --tail=100 bot
curl -fsS https://botatomic.atomicshop.ir/ready
```

مهاجرت‌های دیتابیس در شروع ربات به‌شکل idempotent اجرا می‌شوند و اطلاعات
فعلی PostgreSQL را حذف نمی‌کنند. تنظیمات جدیدی که از پنل تغییر می‌کنند داخل
جدول `BotSettings` ذخیره می‌شوند و بعد از restart باقی می‌مانند.

برای زرین‌پال، مقدار callback باید یک آدرس HTTPS عمومی باشد؛ مقدار پیشنهادی:

```text
https://botatomic.atomicshop.ir
```

ربات مسیر کامل callback هر سفارش را خودش می‌سازد. لینک پرداخت هم به‌صورت
متن قابل کپی و هم دکمه مستقیم «باز کردن درگاه پرداخت» نمایش داده می‌شود.
